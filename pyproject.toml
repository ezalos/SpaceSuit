[project]
name = "setup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "ezpy-logs>=0.1.2",
    "pydantic>=2.10.6",
    "pytest-ordering>=0.6",
    "fire>=0.7.0",
    "pyyaml>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
